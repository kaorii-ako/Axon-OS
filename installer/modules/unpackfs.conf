# Calamares unpackfs module — Axon OS
# Unpacks the live SquashFS image onto the target partition.
# casper mounts the live medium at /cdrom.

---

unpack:
  - source: "/cdrom/casper/filesystem.squashfs"
    sourcefs: "squashfs"
    destination: ""
