# Calamares packages module — Axon OS
# No package operations during install; the live image already contains
# everything. Kept in the sequence so future remove/install lists can be
# added here.

---

backend: apt
skip_if_no_internet: true
update_db: false
operations: []
