<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
  <policy context="default">
    <!-- Allow owner to register service -->
    <allow own="org.axonos.Brain"/>
    <!-- Allow clients to send messages to Brain service -->
    <allow send_destination="org.axonos.Brain"/>
    <!-- Allow receiving messages/signals from Brain service -->
    <allow receive_sender="org.axonos.Brain"/>
  </policy>
</busconfig>
